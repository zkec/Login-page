//animation class after the page has loaded :)
document.addEventListener("DOMContentLoaded", function () {
  const loginBox = document.querySelector(".login-box");
  loginBox.classList.add("animated");
});